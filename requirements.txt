# --- HTTP / env ---
requests>=2.34
python-dotenv>=1.2

# --- data handling ---
pandas>=3.0
numpy>=2.4
pyarrow>=25.0          # parquet read/write for pandas

# --- GRIB2 / NWP decoding ---
xarray>=2026.7
cfgrib>=0.9.15          # wraps ecCodes to read GRIB2 into xarray
ecmwf-opendata>=0.3.34   # thin client for ECMWF's open-data GRIB feed

# --- ENTSO-E client ---
entsoe-py>=0.8

# --- visualization ---
plotly>=7.0
streamlit>=1.63
matplotlib>=3.11

# --- stats ---
scikit-learn>=1.9
scipy>=1.17            # pearsonr (src/analysis.py price/residual-load correlation)
pymc>=5.28             # Bayesian regression (notebooks/generation_model.ipynb)
arviz>=0.23             # posterior diagnostics (R-hat, ESS) and credible intervals

# --- notebooks (notebooks/generation_model.ipynb) ---
jupyter>=1.1
ipykernel>=7.3
nbconvert>=7.17

